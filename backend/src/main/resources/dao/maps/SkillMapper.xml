<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skillhub.dao.SkillMapper">

    <resultMap id="SkillMap" type="com.skillhub.domain.Skill">
        <id     property="id"               column="id"/>
        <result property="name"             column="name"/>
        <result property="description"      column="description"/>
        <result property="packageUrl"       column="package_url"/>
        <result property="status"           column="status"/>
        <result property="developer"        column="developer"/>
        <result property="downloadCount"    column="download_count"/>
        <result property="useCount"         column="use_count"/>
        <result property="visibilityType"   column="visibility_type"/>
        <result property="visibilityConfig" column="visibility_config"/>
        <result property="delisted"         column="is_delisted"/>
        <result property="delistedReason"   column="delisted_reason"/>
        <result property="delistedAt"       column="delisted_at"/>
        <result property="delistedBy"       column="delisted_by"/>
        <result property="createdAt"        column="created_at"/>
        <result property="updatedAt"        column="updated_at"/>
        <result property="lastPublishedAt"  column="last_published_at"/>
    </resultMap>

    <resultMap id="SkillWithVersionsMap" type="com.skillhub.domain.Skill" extends="SkillMap">
        <collection property="versions" ofType="com.skillhub.domain.SkillVersion"
                    column="{skillId=id}" select="com.skillhub.dao.SkillVersionMapper.selectBySkillId"/>
    </resultMap>

    <sql id="skillColumns">
        id, name, description, package_url, status, developer, download_count, use_count,
        visibility_type, visibility_config, is_delisted, delisted_reason, delisted_at, delisted_by,
        created_at, updated_at, last_published_at
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO skill (name, description, package_url, status, developer,
                           visibility_type, visibility_config, updated_at, last_published_at)
        VALUES (#{name}, #{description}, #{packageUrl}, #{status}, #{developer},
                #{visibilityType}, #{visibilityConfig}, #{updatedAt}, #{lastPublishedAt})
    </insert>

    <select id="selectById" resultMap="SkillWithVersionsMap">
        SELECT <include refid="skillColumns"/> FROM skill WHERE id = #{id}
    </select>

    <select id="selectByName" resultMap="SkillMap">
        SELECT <include refid="skillColumns"/> FROM skill WHERE name = #{name}
    </select>

    <select id="existsByName" resultType="boolean">
        SELECT COUNT(*) > 0 FROM skill WHERE name = #{name}
    </select>

    <select id="countByNameExcludingId" resultType="int">
        SELECT COUNT(*) FROM skill WHERE name = #{name} AND id != #{id}
    </select>

    <update id="update">
        UPDATE skill
        SET name = #{name}, description = #{description}, package_url = #{packageUrl},
            status = #{status}, developer = #{developer},
            visibility_type = #{visibilityType}, visibility_config = #{visibilityConfig},
            download_count = #{downloadCount}, use_count = #{useCount},
            is_delisted = #{delisted}, delisted_reason = #{delistedReason},
            delisted_at = #{delistedAt}, delisted_by = #{delistedBy},
            updated_at = #{updatedAt}, last_published_at = #{lastPublishedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM skill WHERE id = #{id}
    </delete>

    <select id="selectAll" resultMap="SkillMap">
        SELECT <include refid="skillColumns"/> FROM skill ORDER BY created_at DESC
    </select>

    <select id="selectByStatus" resultMap="SkillMap">
        SELECT <include refid="skillColumns"/> FROM skill WHERE status = #{status}
    </select>

    <select id="selectPublishedAndNotDelisted" resultMap="SkillMap">
        SELECT <include refid="skillColumns"/> FROM skill
        WHERE status = 'PUBLISHED' AND (is_delisted IS NULL OR is_delisted = false)
    </select>

    <!-- 分页查询 -->
    <select id="selectByStatusAndName" resultMap="SkillMap">
        SELECT <include refid="skillColumns"/> FROM skill
        WHERE status = #{status} AND name LIKE CONCAT('%', #{name}, '%')
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectByStatusOnly" resultMap="SkillMap">
        SELECT <include refid="skillColumns"/> FROM skill
        WHERE status = #{status}
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectByNameOnly" resultMap="SkillMap">
        SELECT <include refid="skillColumns"/> FROM skill
        WHERE name LIKE CONCAT('%', #{name}, '%')
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectPage" resultMap="SkillMap">
        SELECT <include refid="skillColumns"/> FROM skill
        ORDER BY created_at DESC LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByStatusAndName" resultType="long">
        SELECT COUNT(*) FROM skill WHERE status = #{status} AND name LIKE CONCAT('%', #{name}, '%')
    </select>

    <select id="countByStatus" resultType="long">
        SELECT COUNT(*) FROM skill WHERE status = #{status}
    </select>

    <select id="countByName" resultType="long">
        SELECT COUNT(*) FROM skill WHERE name LIKE CONCAT('%', #{name}, '%')
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM skill
    </select>

    <update id="incrementDownload">
        UPDATE skill SET download_count = download_count + 1 WHERE id = #{id}
    </update>

    <update id="incrementUse">
        UPDATE skill SET use_count = use_count + 1 WHERE id = #{id}
    </update>

</mapper>
# Skill Hub 手动测试样例

> 前置：后端 http://localhost:8080 已启动，浏览器打开 http://localhost:3000

---

## 样例 1：创建 → 发布 → 审批通过（完整正向流程）

1. 点击 **技能管理** Tab → **创建技能**
2. 填写：
   - 名称：`数据清洗工具`
   - 描述：`支持CSV/JSON格式的清洗和转换`
   - 开发者：`张三`
   - 可见范围：全公司（默认）
3. 点击 **创建** → 看到 `草稿` 状态
4. 点击 **发布** → 填写更新日志 `初始版本` → **提交**
5. 状态变为 `审批中`，发布按钮消失
6. 点击 **管理台** Tab → 看到待审批记录
7. 点击 **通过** → 状态变为 `已发布`，版本 `1` 出现
8. 回到技能管理，看到 `已发布` 标签

---

## 样例 2：审批拒绝 → 编辑 → 重新提交

9. 创建新技能 `日志分析工具`，提交审批
10. 管理台 → 点击 **拒绝** → 弹出输入框 → 输入理由：`缺少安全性说明`
11. 回到技能管理 → 状态变为 `未通过`，发布按钮重新出现
12. 点击 **编辑** → 补充描述 `增强数据安全校验` → **更新**
13. 点击 **发布** → 提交审批
14. 管理台 → **通过**
15. 点击 **查看** → 审批历史显示一条"拒绝"、一条"通过"

---

## 样例 3：从未发布的技能可直接删除

16. 创建新技能 `废弃项目`，不发布
17. 点击 **删除** → 确认 → 技能消失

---

## 样例 4：发布过的技能不能删除，只能下架

18. 找到已发布的 `数据清洗工具`，确认没有 **删除** 按钮
19. 切换到 **管理台** Tab → 点击 **技能下架/恢复** 子 Tab
20. 找到 `数据清洗工具` → 点击 **下架** → 输入原因 `版本过旧` → 确定
21. 技能状态变为 `已下架`
22. 同页面点击 **恢复** → 技能回到 `已发布`

---

## 样例 5：审批中编辑技能 → 自动作废

23. 创建新技能 `文档生成器`，提交审批
24. 技能变为 `审批中`
25. 点击 **编辑** → 改名 `文档生成器 Pro` → **更新**
26. 技能状态回到 `草稿`（旧审批自动作废）
27. 管理台该请求显示状态为 REJECTED，理由包含"已变更"

---

## 样例 5b：审批中点保存 → 自动作废

28. 创建新技能 `报告分析工具`，提交审批
29. 技能变为 `审批中`
30. 点击 **保存** → 状态回到 `草稿`（旧审批自动作废）
31. 管理台管理员点 **通过** → 提示"该申请不在待审批状态"

---

## 样例 6：发布后不编辑不能再次发布

32. 把 `文档生成器 Pro` 重新提交审批并通过
33. 状态 `已发布`，没有 **发布** 按钮
34. 编辑技能 → 改描述 → 更新
35. **发布** 按钮重新出现 → 可再次提交审批

---

## 样例 7：通知查看

36. 点击右上角 **通知** 铃铛
37. 看到所有审批结果通知（通过/拒绝）
38. 未读通知有红点标识
